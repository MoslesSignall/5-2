; 第08题：键盘输入一个二进制数（字类型），以十六进制的形式输出。
; Coded by: Mosles Signall
; Date: 2025-Oct-23

DATAS SEGMENT
    ADDITION DW 0
DATAS ENDS

STACKS SEGMENT
    DW 100 DUP(?)
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX

    MOV BX,2
INPUT:
    MOV AH,1
    INT 21H
    MOV AH,0
    CMP AL,0DH
    JE INPUT_END
    SUB AL,30H
    PUSH AX
    POP CX
    MOV AX,ADDITION
    MUL BX
    MOV ADDITION,AX
    ADD ADDITION,CX
    JMP INPUT

INPUT_END:
    MOV CX,4
    MOV BX,16
    MOV AX,ADDITION

DIVIDE:
    MOV DX,0
    DIV BX
    ADD DX,30H
    CMP DX,39H
    JA ADD07
    ADD07_END:
    PUSH DX
    LOOP DIVIDE

    MOV CX,4
    MOV AH,2
DISPLAY:
    POP DX
    INT 21H
    LOOP DISPLAY

EXIT:
    MOV AH,4CH
    INT 21H

ADD07:
    ADD DX,07H
    JMP ADD07_END
CODES ENDS
    END START